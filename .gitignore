# .NET build output
bin/
obj/
*.user

# Local coordinator data (world archives + state.json) — never commit saves
data/

# Release artifacts (built by CI)
publish/
installer/output/

# IDE
.vs/
.idea/
